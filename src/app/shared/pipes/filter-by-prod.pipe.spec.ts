import { FilterByProdPipe } from './filter-by-prod.pipe';

describe('FilterByProdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByProdPipe();
    expect(pipe).toBeTruthy();
  });
});
